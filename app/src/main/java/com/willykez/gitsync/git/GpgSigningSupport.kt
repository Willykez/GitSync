package com.willykez.gitsync.git

/**
 * Placeholder until JGit's GPG signing module is added.
 *
 * HTTPS authentication, cloning, pushing and pulling continue to work normally.
 * Signed commits are simply unavailable.
 */
object GpgSigningSupport {

    fun ensureRegistered() {
        // No-op
    }
}