<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Matches ApkInstaller.CACHE_SUBFOLDER — this is the only folder the
         FileProvider grants access into, not the whole app cache. -->
    <cache-path name="apk_installs" path="apk-installs/" />
</paths>
